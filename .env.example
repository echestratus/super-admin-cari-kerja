# Vite (baked into the image at docker build time — also set via compose build.args)
VITE_API_BASE_URL=https://be-stage.cari-kerja.co.id/api/v1
VITE_APP_NAME=Job Portal

# Optional local ops only — GitHub Actions uses repository secrets + SSH private key
# VPS_HOST=
# VPS_PORT=22
# VPS_USER=root
# Never commit real passwords or private keys.
